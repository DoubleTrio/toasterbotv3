// import { CommandSystem } from "../structures";
// jest.mock('fs');

// describe('CommandSystem', () => {
//     const commandSystem = new CommandSystem();
//     test('initial command and aliases size to be 0', () => {
//         expect(commandSystem.aliases.size).toBe(0);
//         expect(commandSystem.commands.size).toBe(0);
//     });

//     test('', () => {
//         commandSystem.loadCommands();
//         // console.log(commandSystem.aliases);
//     });
// });
